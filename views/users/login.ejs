<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">

<a class="navbar-brand" href="/dashboard">
    <i class="fa-solid fa-laptop-code" style="font-size: 1.5rem;"></i>
    <span class="ms-2"><b>SkillColab</b></span>
</a>

<div class="login-page">
    <h1>Login</h1>

    <div class="container">
        <div class="heading">Sign In</div>
        <form action="/login" method="POST" class="form">
            <input type="text" class="input" id="username" name="username" placeholder="Username" required>
            <input type="password" class="input" id="password" name="password" placeholder="Password" required>
            <input class="login-button" type="submit" value="Sign In">
        </form>
        <span class="agreement"><a href="/register">Create new Account</a></span>
    </div>
</div>

<style>
    html, body {
        margin: 0;
        padding: 0;
        height: 100%;
    }

    .navbar-brand {
        display: flex;
        align-items: center;
        position: absolute;
        left: 20px;
        top: 20px;
        color: #84dbf3;
        text-decoration: none;
    }

    .navbar-brand span {
        font-weight: bold;
        font-size: 1.2rem;
    }

    .navbar-brand i {
        font-size: 1.5rem;
        margin-right: 8px; /* Space between the icon and text */
    }


    /* Full Page Gradient Styling */
    .login-page {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        width: 100%;
        background: linear-gradient(135deg, rgba(10, 28, 64, 0.9), rgba(38, 116, 160, 0.7));
        text-align: center;
        flex-direction: column;
    }

    /* Title Styling */
    h1 {
        font-weight: bold;
        font-size: 40px;
        color: black;
        margin-bottom: 50px;
    }

    /* Container Styling (form container) */
    .container {
        max-width: 300px;
        background: #F8F9FD;
        background: linear-gradient(135deg, rgba(10, 28, 64, 0.9), rgba(38, 116, 160, 0.7));
        border-radius: 15px;
        padding: 30px 40px;
        box-shadow: rgba(133, 189, 215, 0.5) 0px 30px 30px -20px;
        width: 100%;
        text-align: center;
    }

    /* Heading Styling */
    .heading {
        font-weight: 900;
        font-size: 30px;
        color: rgb(16, 137, 211);
        margin-bottom: 20px;
    }

    /* Form and Inputs Styling */
    .form {
        margin-top: 20px;
    }

    .form .input {
        width: 100%;
        background: white;
        border: none;
        padding: 15px 20px;
        border-radius: 20px;
        margin-top: 15px;
        box-shadow: #cff0ff 0px 10px 10px -5px;
        border-inline: 2px solid transparent;
    }

    .form .input::placeholder {
        color: rgb(170, 170, 170);
    }

    .form .input:focus {
        outline: none;
        border-inline: 2px solid #12B1D1;
    }

    /* Button Styling */
    .form .login-button {
        display: block;
        width: 100%;
        font-weight: bold;
        background: linear-gradient(45deg, rgb(16, 137, 211) 0%, rgb(18, 177, 209) 100%);
        color: white;
        padding-block: 15px;
        margin: 20px auto;
        border-radius: 20px;
        box-shadow: rgba(133, 189, 215, 0.5) 0px 20px 10px -15px;
        border: none;
        transition: all 0.2s ease-in-out;
    }

    .form .login-button:hover {
        transform: scale(1.03);
        box-shadow: rgba(133, 189, 215, 0.5) 0px 23px 10px -20px;
    }

    .form .login-button:active {
        transform: scale(0.95);
        box-shadow: rgba(133, 189, 215, 0.5) 0px 15px 10px -10px;
    }

    /* Create New Account Link Styling */
    .agreement {
        display: block;
        margin-top: 15px;
    }

    .agreement a {
        text-decoration: none;
        color: #000;
        font-size: 1rem;
    }
</style>
